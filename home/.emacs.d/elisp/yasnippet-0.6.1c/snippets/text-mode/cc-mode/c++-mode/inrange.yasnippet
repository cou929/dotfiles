# -*- mode: snippet -*-
# name: inrange
# key: inrange
# group: c++-mode
# --
bool inRange(int ${1:x}, int ${2:y}, int ${3:n}, int ${4:m}) {
  if (0 <= $1 && $1 < $3 && 0 <= $2 && $2 < $4)
    return true;
  return false;
}
$0